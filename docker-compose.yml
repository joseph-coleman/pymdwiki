{
"services": {
  "pymdwikiserver": {
    "build": ".",
    "container_name": "pymdwiki",
    "command": "uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "volumes": [
      "${APP_LOCATION}:/app/app",
      "${DOCUMENTS_LOCATION}:/mnt/data"
        ],
    "ports":
      ["8000:8000"],
    "environment":
      ["PYTHONUNBUFFERED=1"]
  }
}
}